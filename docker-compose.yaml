version: '3.8'
services:
  fitcafe:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"
    restart: unless-stopped
    networks:
      - coolify
networks:
  coolify:
    external: true
    name: coolify
